coalesce ui will load when:
- cold starting obsidian, all notes must load coalesce ui
- loading a new note into an existing pane (in view mode).
- loading a new note (in view mode).
- going from edit into view mode.

coalesce ui will not load when:
- clicking on a visible note with coalesce ui already loaded and visible.